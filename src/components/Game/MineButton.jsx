import * as React from "react";
export function MineButton({
  columns,
  rows,
  isBomb,
  isPressed,
  isFlagged,
  value,
}) {
  return (
    <button type="button" className="gameCell">
      6
    </button>
  );
}
